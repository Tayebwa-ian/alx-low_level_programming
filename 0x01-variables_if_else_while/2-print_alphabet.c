#include <stdio.h>
/**
 * main - program entry point.
 *
 * Description: "Printing alphabet in lowercase",
 *
 * Return: 0 (Success)
 */
int main(void)
{
for(char letter = 'a'; &letter <= "z"; letter++)
{
putchar(letter);
}
return (0);
}
